# CPC Digital Catalogue — Project State

Last updated: 2026-09-20 10:51 IST (UTC+05:30)

This is the primary recovery document for the project. Read this file before substantial development work, then inspect the current Git implementation before making changes.

## Product goal

Build a premium, easy-to-use Cambridge Publishing Company digital catalogue that lets customers discover publications, select books/custom kits, set quantities, submit an order request, and gives CPC a reliable operational request record.

This is a catalogue/request system, not an e-commerce checkout in V1.

## Current customer journey

Implemented and working:

1. Homepage / catalogue entry
2. Early Learning browsing
3. School Education browsing
4. School book lists
5. Book Details
6. Build Your Own Kit
7. Shared My Selection
8. Quantity editing and validation
9. Request Details form
10. Review Request
11. Send Request through Supabase Edge Function
12. Success state with CPC request reference

Verified real end-to-end submission:

- `CPC-20260816-5645CC`
- 8 top-level selections
- total quantity 18
- included a custom LKG kit
- Supabase correctly stored request, request items and kit components

## Backend state

Supabase project: **CPC Digital Catalogue**.

Implemented:

- `requests`
- `request_items`
- `request_kit_components`
- `product_mappings`
- atomic `submit_catalogue_request(jsonb)` database function
- `submit-catalogue-request` Edge Function
- RLS enabled on business tables
- browser roles cannot directly read/write request tables
- internal SKU/Tally mappings are resolved server-side rather than trusted from browser input

Product mapping data itself is intentionally not populated yet.

## Catalogue data model — current state

Canonical schema migration is complete in source and the previously broken Kit Builder was live-tested successfully by the user on 2026-08-16.

- `class` is now the single Nursery–Class 10 classification field.
- `class` is always an array.
- legacy catalogue `levels` records were removed from `catalogue-data.js`.
- Early Learning common titles explicitly use `["Nursery", "LKG", "UKG"]` to preserve the previous availability behaviour.
- Draw & Colour A/B/C map respectively to Nursery/LKG/UKG.
- all current school textbook records are explicitly English medium, including Kannada/Hindi language subjects.
- current exam/guide records use English/Kannada medium only; legacy Hindi-medium values were removed.
- `catalogue-query.js` queries class arrays without legacy level compatibility.
- `catalogue-validator.js` validates the canonical array model and rejects a legacy `levels` field.
- Kit Builder and Book Details work with class arrays.
- School Books and Exam Preparation use the shared canonical class-aware query layer.
- request submission flattens class arrays safely for the current backend snapshot fields.

Current catalogue record count remains structurally equivalent to the prior source: 150 records are generated by the canonical source definitions.

## Current publication-browser UX

As of 2026-09-20 10:51 IST:

- `css/publication-browser.css` is the shared presentation contract for publication cards.
- School Books and School Exam Preparation use cover-led multi-column cards on desktop.
- On mobile, those publication cards become compact horizontal cards with cover left and information/actions right.
- College and University and Competitive Exam publication browsers use the same shared card system when titles exist.
- These publication browsers provide a direct `Home` route plus the contextual parent route.
- The previous one-book-per-full-width-row pattern should not be reintroduced without a specific UX reason.
- Live GitHub Pages visual/device verification of this 10:51 UX patch is still pending.

## Current priority

Do **not** continue product mapping/staff workflow yet.

The College and University and Competitive Exams navigation and publication-browser shells are implemented and linked from the homepage. Both use the central catalogue dataset and show honest empty states because no verified PUC, Degree, or competitive-exam titles have been added yet.

The homepage search, Browse All Series and Subjects & Book Types route into one shared `browse.html` discovery page. It searches the existing central catalogue and provides filters from actual category, class, series, subject and book-type values.

**IMMEDIATE NEXT:** visually verify the new shared publication-card layout and Home/context navigation on desktop and mobile, then apply/fix consistency issues discovered during the audit before adding more features.

After that: obtain verified CPC title data for 1st PUC, 2nd PUC, Degree and Competitive Exams, define minimum taxonomy from actual titles, then populate and test the browsers and shared Book Details/selection flow.

Current roadmap:

1. Verify/refine shared publication-card + navigation UX in browser/mobile
2. Verified College and University / Competitive Exams data and taxonomy
3. Verify/refine shared Browse + Search page
4. Full regression testing
5. Populate product mappings (SKU / ISBN / Tally Item Name / internal identifiers)
6. Staff request dashboard + notifications

## Important recovered decisions

- Do not maintain both `level` and `class` for Nursery–Class 10.
- Use **`class` only** in catalogue product data.
- `class` supports multiple applicable classes via an array.
- Early Learning school-stage values are Nursery, LKG and UKG. Playgroup still requires real product-data confirmation.
- School values are 1–10.
- Subject and medium are separate concepts.
- A Kannada subject book is **not** automatically Kannada medium.
- Regular textbooks are English medium.
- Kannada-medium editions are used only where an actual Kannada-medium publication exists.
- Do not infer medium from subject.

## Navigation/UX rules already learned

- Internal catalogue pages should expose a compact direct Home route plus the contextual parent/back route where practical.
- Back navigation on internal/section pages should be consistently placed at the top-left of the content area; do not center the Back control just because the hero content is centered.
- Preserve browser-native Back/scroll restoration for ordinary View Book navigation.
- Do not persist stale scroll positions and replay them on refresh/revisit.
- My Selection is shared across catalogue sections.
- Quantity limits currently allow 1–10,000 for selected request lines; invalid input should be explained rather than silently producing confusing values.
- `0` must support clearing/removing a selection through the intended selection UX rather than requiring minus-button gymnastics.
- Preserve working Review Request UI; backend integration is already complete.

## Homepage gaps

The homepage already visually contains:

- College and University (linked)
- Competitive Exams (linked)
- top catalogue search (linked to shared Browse)
- Browse All Series (linked to shared Browse)
- Subjects & Book Types (linked to shared Browse)

Featured-book buttons are still placeholders. Do not redesign working sections unnecessarily; connect remaining journeys through shared architecture.

## Development definition of done

For meaningful changes:

**Inspect current Git → read relevant docs → implement smallest coherent change → test affected flows → verify → update docs → commit.**

Do not claim a feature is complete solely because code was written.
